#include "main.h"
/**
 * _sqrt_recursion - return sqrt
 * @n: int
 *
 * Return: int
 */
int _sqrt_recursion(int n)
{
	if (n < 0)
		return (-1);
	if (i * i == n)
		return (i);

	_sqrt_recursion(i - 1);
	return (-1);
}
