#include "main.h"
/**
 * print_sign - return abs value of a given number
 * @n: interge to test
 * Return: abs value of n
 */
int _abs(int n)
{
	if (n < 0)
	{
		return -n;
	}
	else
	{
		return n;
	}
}
